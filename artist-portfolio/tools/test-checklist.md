# Test Checklist

Manual verification checklist keyed to requirement IDs. Updated per segment.

| ID | Requirement | Status |
| --- | --- | --- |
| FR-01 | Hero with artist name and one-line statement | not covered |
| FR-02 | Work grid of 9–12 pieces | S04: manual check — serve via `python -m http.server 8080`; Console `document.querySelectorAll("[data-grid] [data-lightbox-trigger]").length` → 12; each tile has `data-id`, `data-tilt`, `data-lightbox-trigger`, an SVG with `<title>`, `.tile__sheen` and a caption (title + medium · year). Device toolbar 320 / 768 / 1280 px → 1 / 2 / 3 columns, no horizontal overflow. Tab through grid: accent focus ring + sheen on each tile. `Renderer.render([{ id: "x" }], grid)` → one `console.warn`, zero tiles |
| FR-03 | Lightbox on tile click | not covered |
| FR-04 | About section | not covered |
| FR-05 | Contact section (mailto + social links) | not covered |
| FR-06 | Tile tilt toward cursor (max 8°) with specular sheen | not covered |
| FR-07 | Pointer-following soft light with proximity lift | S06 (architecture FR-08, NFR-03, NFR-04): serve via `python -m http.server 8080`, zero console errors/CSP violations. Console: `Capabilities.detect()` → three booleans, `document.documentElement.dataset.motion` → `undefined` on desktop; `typeof spring === "function"`, `spring(0, 10, 0)` → `[1.35, 1.35]`; `PointerLight.RADIUS` → 320, `PointerLight.LIFT_MAX` → 18. Move pointer over the grid: `getComputedStyle(document.querySelector("[data-pointer-light]")).opacity` → `"1"` and `.style.getPropertyValue("--lx")` is a px value tracking the cursor. Hover near a tile: `tile.style.getPropertyValue("--lift")` between `1px` and `18px`; far tiles `0px`. Stop moving ~1 s, wrap `requestAnimationFrame` and count for 1 s → 0 calls (loop self-suspends, idle CPU zero). Source audit: `getBoundingClientRect` only inside `cacheRects()`; `pointermove` handler has no DOM reads/writes. Resize + scroll, hover again → lift triggers on the correct tile (rect cache refreshed after ≈150 ms). Performance → CPU 4× → 5 s pointer movement → no frame > 16.7 ms after activation; Rendering → Paint flashing → no tile repaints. `document.documentElement.dataset.theme = "light"` → glow still subtle, no muddy blend |
| FR-08 | Scroll-triggered fade-and-rise via IntersectionObserver | S08 (architecture FR-09, FR-11 reveal part, NFR-09): manual check — serve via `python -m http.server 8080`, zero console errors/CSP violations on load and during scroll. Console: `document.querySelectorAll("[data-reveal]").length` → 4; `Reveal.THRESHOLD` → 0.15, `Reveal.ROOT_MARGIN` → `"0px 0px -10% 0px"`. On load `#hero` (and `#work` if in view) has class `is-visible`; `#about`/`#contact` do not until scrolled into view. Before scroll `getComputedStyle(document.querySelector("#contact")).opacity` → `"0"` and `.transform` → `matrix(1, 0, 0, 1, 0, 24)`; after scroll `"1"` / `none`. Scroll away and back → class persists (once-only); after all four revealed `Reveal.observer` → `null`. `Reveal.init([], Capabilities.detect())` → no error, no observer. Fallback: `Reveal.showAll(document.querySelectorAll("[data-reveal]"))` → all four `.is-visible`; source audit confirms the `!caps.hasIO` branch calls `showAll` and creates no observer; observer callback does only `classList.add` + `unobserve` (no `getBoundingClientRect`, `querySelector`, scroll/resize listeners). JS disabled → reload → all sections visible, no `html.js` class, noscript captions shown. Device toolbar (coarse pointer) → sections still reveal on scroll. Performance → CPU 4× → scroll through page → no frame > 16.7 ms attributable to reveal; Paint flashing → only compositor transition |
| FR-09 | Anchor navigation between sections | S09 (architecture FR-14, NFR-03, NFR-06, NFR-09): serve via `python -m http.server 8080`, zero console errors/CSP violations on load and while scrolling. Console: `document.querySelectorAll("[data-nav-link]").length` → 3 (hrefs `#work`, `#about`, `#contact`); `getComputedStyle(document.querySelector("[data-nav]")).position` → `"sticky"`, `.zIndex` → `"10"` (above `.pointer-light` z-index 0 and tiles). Scroll to `#about` → `document.querySelector('[data-nav-link][href="#about"]').getAttribute("aria-current")` → `"true"`, the other two → `null`; `getComputedStyle(currentLink).opacity` → `"1"`, non-current → `"0.6"`; exactly one link carries `aria-current` at any time. `Nav.ROOT_MARGIN` → `"-40% 0px -55% 0px"`; `Nav.init([], [], Capabilities.detect())` → no error; `Nav.init(links, sections, { hasIO: false })` → `Nav.observer` stays `null`. Source audit: observer callback calls only `setCurrent` (attribute toggling); no `querySelector`/`getBoundingClientRect`/scroll/resize listeners in `Nav`; `.nav__link` transition is `opacity var(--dur-fast) var(--ease-out)` only. Device toolbar 320 / 768 / 1280 / 2560 px → header fits, no horizontal scrollbar, links ≥ 44 px tall (brand and links wrap at 320 px). DevTools → disable JavaScript → reload → header anchors still smooth-scroll to sections; no link hidden. Performance → CPU 4× → scroll 5 s → no frame > 16.7 ms attributable to nav; Paint flashing shows compositor-only changes on link opacity |
| FR-10 | Theme switch via `data-theme` | S09 (architecture FR-12): Console `document.documentElement.dataset.theme = "light"` → `getComputedStyle(document.body).backgroundColor` → `rgb(244, 241, 234)`; `getComputedStyle(document.documentElement).getPropertyValue("--color-accent").trim()` → `#8a5a1c`; body, tiles (glass panel with inner highlight, low-opacity border, restrained shadow), sticky nav, pointer light and focus rings (Tab to a link) recolour with no JS involvement and no console output; nav/tile text legible on bone white. Set back to `"dark"` → `rgb(14, 14, 16)` and accent `#d9a85a`. Editing `<html data-theme="light">` in markup gives the same result on load. Default remains `data-theme="dark"` |
| FR-11 | Placeholder images and intentional copy | S04: manual check — `document.querySelectorAll("[data-grid] svg title").length` → 12; `new Set(ARTWORKS.map(a => a.paletteSeed.split("-")[0])).size >= 4` and same for `[1]` → true (5 hues × 4 compositions used); `Renderer.placeholderSVG("bogus", "a<b")` warns and returns markup containing `a&lt;b`; visually distinct flat-colour compositions, no multi-stop gradients; toggle `document.documentElement.dataset.theme = "light"` and confirm tiles still read; titles/media/dimensions/years read as genuine metadata (no lorem ipsum) |
| FR-12 | Keyboard-navigable interactive elements | not covered |
| FR-13 | Lightbox keyboard/escape handling | not covered |
| FR-14 | noscript captions fallback | S04: manual check — DevTools → disable JavaScript, reload: `<noscript>` list of 12 "Title — medium, dimensions, year" captions visible and matching `ARTWORKS` |
| NFR-01 | Only external resource is the web-font link | covered by tools/check-constraints.mjs |
| NFR-02 | No external scripts / libraries | covered by tools/check-constraints.mjs |
| NFR-03 | Animate only transform/opacity | S08: manual check — reveal rules `html.js [data-reveal]` / `.is-visible` transition only `opacity` and `transform` via `var(--dur-base) var(--ease-out)`; no `@keyframes`; verified mechanically by `node tools/check-constraints.mjs index.html` → `OK` |
| NFR-04 | 60 fps under 4x CPU throttle | S11 (architecture NFR-04, NFR-05): serve via `python -m http.server 8080`, open in Chrome, zero console errors. **Frame time:** DevTools → Performance → gear → CPU: 4× slowdown → Record → 5 s of continuous pointer movement across the whole work grid, then 5 s of scrolling (wheel + keyboard) → Stop. Pass if no frame > 20 ms after the first `pointerenter` frame (the activation frame that toggles `.is-active` may be excluded). **Console timing snippet** (run with 4× throttle still on, start it and then move the pointer for 5 s; expect `max frame ms` ≤ 20): `(() => { const end = performance.now() + 5000; let last = performance.now(), max = 0; const f = (t) => { max = Math.max(max, t - last); last = t; if (t < end) requestAnimationFrame(f); else console.log("max frame ms", max.toFixed(1)); }; requestAnimationFrame(f); })()`. **Layer audit:** More tools → Rendering → Layer borders → exactly one composited layer for `.pointer-light` (static `will-change: transform`); tiles must NOT be permanently promoted (no orange border on idle tiles; a tile may be promoted only while its `transform` transition is running). Alternatively Layers panel → count layers with pointer idle. **Paint flashing:** Rendering → Paint flashing → move pointer across the grid → no green flashes on tiles (compositor-only `--lift`/`--lx`/`--ly` updates); a flash is allowed only on the initial `.is-active` opacity change. **Source audit (mechanical):** `node tools/check-constraints.mjs` → `INFO: index.html is N.N KB`, `OK` proves no layout reads (`getBoundingClientRect`, `querySelector`, `scrollX/Y`, `offset*`, `client*`, `inner*`, `getComputedStyle`) inside `tick`/`_onMove`, no static `will-change` outside `.pointer-light`, and that `cancelAnimationFrame(` exists (loop can self-suspend); confirm idle: stop moving 1 s, wrap `requestAnimationFrame` and count for 1 s → 0 calls. **Observation:** `.tile { transition: transform … }` coexists with per-frame `--lift` writes; if frame time fails here, report as Architect escalation candidate for `STYLE: CARD / GLASS`, do not patch locally |
| NFR-05 | Motion disabled under prefers-reduced-motion | S06 (architecture FR-11, light part): Rendering → Emulate `prefers-reduced-motion: reduce` → reload → `Capabilities.detect().reducedMotion === true`, `document.documentElement.dataset.motion === "off"`, `getComputedStyle(document.querySelector("[data-pointer-light]")).display === "none"`, `PointerLight.lightEl === null` (init performed no work); move pointer → `--lx` never set, no tile receives `--lift`. Zeroing of `--dur-*` durations: S08/S11. S08 (reveal part): same emulation → reload → all four `[data-reveal]` sections visible immediately with `is-visible` and no transition; `getComputedStyle(document.documentElement).getPropertyValue("--dur-base").trim()` → `"0ms"` (same for `--dur-fast`); `Reveal.observer === null` (no `IntersectionObserver` created, `showAll` path). `html[data-motion="off"] [data-reveal]` safety-net rule also forces `opacity: 1; transform: none; transition: none`. S11 (architecture NFR-05, hot-path layout reads): the "no layout reads in `pointermove`/rAF" audit is now mechanical — check j in `node tools/check-constraints.mjs` scans every `tick(` body, `_onMove` handler and inline `pointermove` listener for `getBoundingClientRect`, `querySelector`, `getComputedStyle`, `offset*`, `client*`, `scrollX/Y`, `page*Offset`, `inner*`, `scrollTop/Left` and fails with `FAIL: hot path "<name>" reads layout: "<token>" (NFR-05)`; `PointerLight.tick()` now uses cached `scrollX`/`scrollY` refreshed in `cacheRects()` and immediately in the `scroll`/`resize` handler. Manual re-check of FR-07: scroll the page, hover a tile → `--lift` lands on the tile under the cursor |
| NFR-06 | Tilt/light disabled on pointer: coarse | S06 (architecture FR-10, light part): Device toolbar (touch device) → reload → `Capabilities.detect().coarsePointer === true`, `document.documentElement.dataset.motion === "off"`, light element `display: none`, `PointerLight.lightEl === null`; touch/drag over grid → `--lx`/`--lift` never set. Tilt part: S05 |
| NFR-07 | Responsive 320–2560 px | not covered |
| NFR-08 | WCAG AA body text contrast | S02: manual check — DevTools colour picker on `--color-fg`/`--color-muted` vs `--color-bg` in both themes; computed dark 16.9:1 / 7.8:1, light 16.6:1 / 6.1:1 (ratios noted in TOKENS comments). S09: light `--color-accent` `#8a5a1c` vs `--color-bg` `#f4f1ea` → 5.2:1 (≥ 4.5:1 AA; used for link hover text and `--color-focus` rings). Verify with DevTools colour picker after `document.documentElement.dataset.theme = "light"`; dark accent `#d9a85a` unchanged |
| NFR-10 | Total file size under 150 KB excluding web fonts | S11: `node tools/check-constraints.mjs` prints `INFO: index.html is N.N KB` then `OK` (check i is a hard failure above 150 × 1024 bytes: `FAIL: index.html is N.N KB (> 150 KB, NFR-10)`, exit 1). Cross-check with `ls -l index.html` (bash) or `(Get-Item index.html).Length` (PowerShell) → < 153600 bytes. Web fonts are external `<link>`s and are not counted |
| NFR-11 | No console errors / CSP violations | S02: manual check — serve via `python -m http.server 8080`, Console shows zero errors/CSP violations; block `fonts.gstatic.com` and reload to confirm `Fraunces-fallback`/`Inter-fallback` render with minimal reflow. S08: manual check — reload and scroll through all four sections, Console shows zero errors/CSP violations introduced by `Reveal` (also under reduced-motion emulation and coarse-pointer emulation). S09: reload, scroll through all sections, toggle `data-theme` dark ↔ light twice, run under reduced-motion emulation (`--dur-fast` → `0ms`, `aria-current` still switches with no transition) → Console shows zero errors/CSP violations from `Nav` or the theme switch; no `innerHTML`, inline handlers, `fetch` or storage introduced (`node tools/check-constraints.mjs index.html` → `OK`) |
